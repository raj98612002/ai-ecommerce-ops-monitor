# ================================================================
#  packages.yml
#  dbt package dependencies
#  Run: dbt deps   (after setting up the project)
# ================================================================

packages:
  - package: dbt-labs/dbt_utils
    version: [">=1.0.0", "<2.0.0"]
